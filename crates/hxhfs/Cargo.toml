[package]
rust-version.workspace = true
name = "hxhfs"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Safe HFS metadata and resource-fork sidecar support"
publish = false

[lib]
crate-type = ["rlib"]
